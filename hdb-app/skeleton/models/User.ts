// Skeleton: model stub (not used). Present only to mirror structure.
export type User = {
  username: string;
  password?: string;
  age?: number; // Added age field
};
const UserModel = {} as unknown as User;
export default UserModel;
